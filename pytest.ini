[pytest]
# Pytest configuration for Socrates project

# Minimum pytest version
minversion = 7.0

# Test discovery paths
testpaths = tests

# Python files patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers for test categorization
markers =
    unit: Unit tests (fast, isolated, no external deps)
    integration: Integration tests (test component interactions)
    e2e: End-to-end tests (full user workflows)
    slow: Tests that take >1 second
    api: API endpoint tests
    database: Database operation tests
    cache: Cache/redis tests
    auth: Authentication tests
    async: Async operation tests
    mock: Tests using mocks
    requires_api: Tests requiring external API keys
    requires_db: Tests requiring database
    test_isolation: Tests that require isolated execution state
    benchmark: Performance benchmark tests

# Command line options (can be overridden)
addopts =
    --strict-markers
    --strict-config
    --verbose
    -ra
    --maxfail=10
    --tb=short
    --disable-warnings

# Coverage options
[coverage:run]
branch = True
omit =
    */venv/*
    */site-packages/*
    */test_*.py
    */__pycache__/*
    */migrations/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod
    @abc.abstractmethod
    @property

precision = 2
skip_covered = False

[coverage:html]
directory = htmlcov
